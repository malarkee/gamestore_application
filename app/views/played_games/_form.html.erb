<%= form_with(model: played_game) do |form| %>
  <% if played_game.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(played_game.errors.count, "error") %> prohibited this played_game from being saved:</h2>

      <ul>
        <% played_game.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :game_id, style: "display: block" %>
    <%= form.text_field :game_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
